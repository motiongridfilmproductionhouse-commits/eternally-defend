# Assessment Search Animation

## Build
- Replace the simple loading bar with a dark, full-width search visualization inspired by the supplied references.
- Animate a bright line into a larger flowing signal, then branch into illuminated paths and finding dots over a 15-second loop.
- Keep the artist portrait, live assessment stage, and accessible status text visible while the real scan runs.
- Adapt the visualization for phone and desktop sizes, with a static reduced-motion version.

## Technical details
- Add a focused React loading visual inside the existing assessment screen.
- Define reusable semantic colors and CSS keyframes in the global design system.
- Do not alter scanning, result counts, risk logic, pricing, or backend behavior.
- Validate the loading state visually and confirm the app remains error-free.
